query findUniqueRoom($roomId: String!) {
  findUniqueRoom(
    where: {
      id: $roomId
    }
  ) {
    title
    image
    description
    gallery
    events {
      id
      title
      image
      shortDescription
      description
    }
  }
}